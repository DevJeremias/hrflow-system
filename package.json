{
  "name": "hrflow-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/run-workspace.mjs frontend build",
    "lint": "node scripts/run-workspace.mjs frontend lint",
    "verify": "node scripts/run-workspace.mjs frontend build && node scripts/run-workspace.mjs frontend lint"
  }
}
